# Learn Terraform

## Prerequisites

1. Install Terraform:

    ```powershell
    winget install -e --id HashiCorp.Terraform
    ```

1. Verify Terraform installation:

    ```console
    terraform version
    ```
